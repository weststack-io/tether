// Drift detection pipeline orchestrator
// Entry point: runDriftDetection(regulatoryItemId)
// Coordinates relevance check, candidate retrieval, classification,
// citation verification, severity scoring, and alert creation.

export async function runDriftDetection(_regulatoryItemId: string): Promise<void> {
  throw new Error("Not implemented");
}
