// Drift classification module
// Calls the Claude API to classify regulatory items against policy chunks.
// Returns structured classification results with confidence scores.

export async function classifyDrift(_regulatoryText: string, _policyText: string): Promise<unknown> {
  throw new Error("Not implemented");
}
